# Context Window

The context window in AI models refers to the amount of input data (tokens) the model can consider at once. A larger context window, like the 1 million tokens in [[DeepSeek V4]], enables the model to handle longer documents, extended conversations, and complex multi-part tasks more effectively.