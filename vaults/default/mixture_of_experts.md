# Mixture-of-Experts (MoE)

Mixture-of-Experts is an architecture where the model consists of multiple expert networks, and only a subset is activated for each input, making it more efficient. [[DeepSeek V4]] uses a trillion-parameter MoE model with approximately 32 billion active parameters per inference.